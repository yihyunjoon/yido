{
  "name": "yido-web",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "pnpm run build:wasm && vite",
    "build:wasm": "wasm-pack build ../yido-wasm --target web --out-dir ../../yido-web/src/wasm/yido_wasm --no-pack",
    "build": "pnpm run build:wasm && tsc -b && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "solid-js": "^1.9.12"
  },
  "devDependencies": {
    "@types/node": "^24.12.3",
    "typescript": "~6.0.2",
    "vite": "^8.0.12",
    "vite-plugin-solid": "^2.11.12"
  }
}
